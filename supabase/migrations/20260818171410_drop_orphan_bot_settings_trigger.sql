-- Fungsi ini berasal dari schema bot_settings lama yang sudah dihapus.
-- Hapus agar tidak menjadi permukaan fungsi publik yang tidak terpakai.
drop function if exists public.set_bot_settings_updated_at();
